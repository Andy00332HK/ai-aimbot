# ByteTrack 設定（基於 ultralytics 預設，加大 track_buffer）
# track_buffer: 30 ≈ 軌跡中斷後仍可重新關聯的幀數（偵測閃斷時 ID 不跳）
tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.1
new_track_thresh: 0.25
track_buffer: 30
match_thresh: 0.8
fuse_score: true
